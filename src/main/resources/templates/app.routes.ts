/**
 * Definición de las rutas de la aplicación Angular.
 * Este archivo exporta un arreglo de objetos de tipo 'Routes' que define las rutas disponibles en la aplicación.
 * En este caso, no se han definido rutas específicas, por lo que el arreglo está vacío.
 */
import { Routes } from '@angular/router';

export const routes: Routes = [];
