export interface User { // Definición de la interfaz User
  id: string; // Propiedad para el ID del usuario
  email: string; // Propiedad para el correo electrónico del usuario
  userName: string; // Propiedad para el nombre de usuario del usuario
}
